# 虚拟机硬件配置工具使用指南

## 概述

本项目包含两个主要脚本：
- `generate-hardware-params.sh`: 用于生成真实的硬件参数配置
- `create-windows-vm.sh`: 用于启动配置了真实硬件参数的Windows虚拟机

## 基本使用方法

### 方法1：自动加载配置（推荐）

```bash
# 使用 --generate 参数自动生成新配置并启动
./create-windows-vm.sh --generate

# 使用 --config 参数从文件加载配置
./create-windows-vm.sh --config vm-config-20260226-1030.txt
```

### 方法2：手动配置

1. 生成硬件参数配置：
   ```bash
   ./generate-hardware-params.sh
   ```

2. 复制输出中的配置变量部分到 `create-windows-vm.sh` 的配置参数部分

3. 运行虚拟机：
   ```bash
   ./create-windows-vm.sh
   ```

## 支持的命令选项

### `create-windows-vm.sh` 选项

- `--generate`: 自动调用 `generate-hardware-params.sh` 生成新配置并加载
- `--config <文件路径>`: 从指定的配置文件中加载硬件参数
- 直接运行: 使用脚本中的默认配置

### 配置文件格式

配置文件必须包含 `# 硬件信息自定义` 部分，格式如下：

```bash
# 硬件信息自定义
BIOS_VENDOR="American Megatrends Inc."
BIOS_VERSION="F15"
BIOS_DATE="07/29/2024"
SYSTEM_MANUFACTURER="MSI"
SYSTEM_PRODUCT="MPG Z490 GAMING CARBON WIFI"
# ... 更多变量
```

## 测试脚本

### `test_variable_match.sh`

用于验证两个脚本之间的变量匹配情况：

```bash
chmod +x test_variable_match.sh
./test_variable_match.sh
```

输出示例：
```
==========================================
  测试变量匹配
==========================================

1. 运行 generate-hardware-params.sh 生成配置...
   ✓ 完成
2. 分析 create-windows-vm.sh 中的变量...
   ✓ 完成

3. 变量匹配情况:
--------------------------
匹配的变量:
  ✓ BIOS_DATE
  ✓ BIOS_VENDOR
  ✓ BIOS_VERSION
  ✓ BOARD_ASSET
  ...

✓ generate-hardware-params.sh 所有变量均已匹配

==========================================
  所有变量匹配成功
==========================================

测试完成!
```

## 高级功能

### 变量默认值保护

`create-windows-vm.sh` 现在支持变量默认值保护机制：
```bash
RAM_BRAND="${RAM_BRAND:-"Samsung"}"
RAM_CAPACITY="${RAM_CAPACITY:-"16GB"}"
# 其他变量...
```

这样即使某些变量未在配置文件中定义，也会使用默认值，避免脚本失败。

### 安全检查

配置加载功能包含安全检查：
- 验证配置文件格式
- 检查配置变量的合法性
- 处理配置文件加载失败的情况

## 配置参数说明

以下是主要配置参数的说明：

### 系统信息 (SMBIOS Type 0-3)
- BIOS_VENDOR, BIOS_VERSION, BIOS_DATE: BIOS信息
- SYSTEM_MANUFACTURER, SYSTEM_PRODUCT, SYSTEM_SERIAL: 系统信息
- BOARD_MANUFACTURER, BOARD_PRODUCT, BOARD_SERIAL: 主板信息
- CHASSIS_MANUFACTURER, CHASSIS_VERSION, CHASSIS_SERIAL: 机箱信息

### 硬件组件信息
- GPU_VENDOR, GPU_MODEL, GPU_VRAM: 显卡信息
- SOUND_CARD, SOUND_VENDOR: 声卡信息
- RAM_BRAND, RAM_CAPACITY, RAM_SPEED: 内存信息
- PSU_BRAND, PSU_MODEL, PSU_WATTAGE: 电源信息
- HDD_MODEL, HDD_SERIAL, HDD_WWN: 硬盘信息

### 网络信息
- MAC_ADDRESS: 网卡MAC地址

## 注意事项

1. 确保 `generate-hardware-params.sh` 和 `create-windows-vm.sh` 具有执行权限：
   ```bash
   chmod +x generate-hardware-params.sh create-windows-vm.sh test_variable_match.sh
   ```

2. 虚拟机镜像文件会自动创建在当前目录下，默认大小为100GB

3. ISO文件默认路径为 `/app/windows_vm/windows.iso`，如需修改请编辑脚本中的 `ISO_PATH` 变量

4. 首次启动会自动从ISO安装Windows系统

5. 虚拟机支持VNC连接（端口：5900）

## 故障排除

### 1. ISO文件不存在错误
```
错误: ISO 文件不存在: /app/windows_vm/windows.iso
```
**解决方法**: 修改 `create-windows-vm.sh` 中的 `ISO_PATH` 变量，指向正确的Windows安装镜像文件。

### 2. KVM加速不可用
```
警告: KVM 不可用，使用软件模拟 (性能较差)
```
**解决方法**: 确保主机系统支持KVM虚拟化，或使用 `--no-accel` 选项禁用加速。

### 3. 配置加载失败
```
警告: 配置文件格式不正确，使用默认配置
```
**解决方法**: 确保配置文件是通过 `generate-hardware-params.sh` 生成的，并且包含正确的 `# 硬件信息自定义` 部分。
